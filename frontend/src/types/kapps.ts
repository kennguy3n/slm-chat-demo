// Phase 0 placeholder for KApp objects (Task, Approval, Form, Artifact).
// Concrete shapes land in Phase 3 alongside the KApp card renderer.
export interface KAppPlaceholder {
  id: string;
  type: string;
}
